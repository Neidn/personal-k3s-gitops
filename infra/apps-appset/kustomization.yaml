# =============================================================================
# infra/apps-appset/kustomization.yaml
#
# ApplicationSet 이 infra/apps-appset 경로를 스캔할 때 렌더링하는 Kustomization
# apps/* 를 자동 스캔하는 ApplicationSet CRD 배포
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - config/appset.yaml
