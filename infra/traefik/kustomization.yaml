# =============================================================================
# infra/traefik/kustomization.yaml
#
# ApplicationSet 이 infra/traefik 을 발견하면 이 kustomization 을 적용
# wave: 0 → argocd(-1) 이후, IngressRoute(1) 이전
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - app.yaml
